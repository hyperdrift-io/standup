# Pick one. Vertex AI (service account, no daily cap):
GOOGLE_SA_KEY_B64=
VERTEX_PROJECT=
VERTEX_LOCATION=global
# Or the Gemini API with a key:
GEMINI_API_KEY=
# Or leave both unset and Strands falls back to Amazon Bedrock with your AWS credentials.
STANDUP_MODEL=gemini-3.6-flash
